import { View, Text, StyleSheet } from "react-native";


export const RotateMessage = () => (
  <View style={styles.rotateMessagePosition}>

  </View>
)

const styles = StyleSheet.create({
  rotateMessagePosition: {
    width: 100,
    height: 100,
    padding: 20,
    backgroundColor: 'red',
  },
})
